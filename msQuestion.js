/* Given A = [51, 71, 17, 42], the function should return 93. 
There are two pairs of numbers whose digits add up to an equal sum: (51, 42) and (17, 71).
 The first pair sums up to 93.
 */

function solution(param){
    if(param.length === 0){
        return {};
    }

// Loop through array
 // if 
}